import React from "react";
import styled from "styled-components";
import Team from "Components/Team";

const Container = styled.div`

`;

const CurrentBattle = ({title, createAt, createBy }) => (
	<Container>
		
	</Container>
);

export default CurrentBattle;